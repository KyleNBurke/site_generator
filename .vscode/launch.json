{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/site_generator",
            "args": ["pages"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}